services:
  hukuk-buro:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      SUPABASE_INTERNAL_URL: http://host.docker.internal:54321
    ports:
      - "3002:3000"
    restart: unless-stopped
